//
//  ascii.c
//  HelloWorld
//
//  Created by Matt Wagner on 3/1/21.
//
/*
#include <stdio.h>

// Global Variables
// Enumerated types

// 2-Step Version
enum months{JAN = 1, FEB, MAR, APR, MAY, JUN, JUL, AUG, SEP, OCT, NOV, DEC};
typedef enum months Month;

enum card{ SPADES, HEARTS, DIAMONDS, CLUBS};
typedef enum card Card;

// 1-Step Version
//typedef enum {JAN = 1, FEB, MAR, APR, MAY, JUN, JUL, AUG, SEP, OCT, NOV, DEC} Month;
//typedef enum { SPADES, HEARTS, DIAMONDS, CLUBS} Card;

int main(int argc, const char* argv[])
{
    char ch;
    Month aMonth;
    Card aPlayingCard;
    
    printf("Please enter a letter: ");
    scanf("%c", &ch);
    
    if(ch >= 'a' && ch <= 'z')  //if(ch >= 95 && ch <= 122)
    {
        printf("A lowercase letter was entered.\n");
    }
    else{
        printf("An non-lower case letter was entered.\n");
    }
    return 0;
}
*/
